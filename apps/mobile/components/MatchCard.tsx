// Shared match card component — renders one match row with status chip, time, ID
// TODO: implement (mirrors web MatchDashboard card)
import { View, Text } from "react-native";

export function MatchCard() {
  return (
    <View>
      <Text>MatchCard</Text>
    </View>
  );
}
